require 'spec_helper'

describe Rake::Unicorn::Rails do
  it 'has a version number' do
    expect(Rake::Unicorn::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
